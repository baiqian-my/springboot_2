1、刚开始配依赖的时候lombok一直显示爆红，问了ai后说可能是版本没跟上，也可能是Maven没搞好，我先尝试看看第一个可能性，发现并没有类似的代码，于是看向第二个问题，是路径的问题重写完路径以后解决了

2、忘了先运行再使用apifox插件

3、想在application.yml文件里试图修改server里的数值，比如8080，但结果是无法运行,问了ai才知道可能是端口被占用，于是还是换回了8888

4、除此以外还有一些小插曲，比如少插件，网络问题，代码出错

5、刚开始学感觉有点跟不上，得一直麻烦同学（他们也有他们的疑问我也不好一直喊他们），所以希望老师可以下次录一下屏。